version: '3.8'
services:
  nginx:
    image: nginx:latest
    container_name: nginx-recordings
    ports:
      - "80:80"
    volumes:
      - /home/casparcg/mediamtx/recordings:/recordings:ro
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
